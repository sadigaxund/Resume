# TODO

- [ ] **Config-driven resume generation** — allow defining Education,
      Experience, Projects, custom sections, links per entry, and icon
      mappings in a YAML or TOML config file, so the template can be reused
      without editing `.tex` directly.
- [ ] **User-configurable full name** — read from a `full_name` file or
      config and inject into the build, so the template works for anyone
      without hardcoding names.
